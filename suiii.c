#include<stdio.h>
#include<conio.h>
int main(){
    int num;

    printf("Enter a number:");
    scanf("%d",&num);
    if ( num == 0)
    printf("This is zero");
    else if ( num < 0)
    printf("negative number");
    else 
    printf("Positive number");






























return 0;
}
